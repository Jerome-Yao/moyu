# 摸鱼时钟 iOS 应用：V1 产品需求

> 状态：需求访谈进行中  
> 更新日期：2026-07-21  
> 本文档是当前需求的唯一事实来源。标为“待确认”的内容不得直接视为最终需求。

## 1. 产品定位

应用名称：`moyu`  
副标题：`实时收入小助手`

一款本地离线运行的 iPhone 原生应用，用实时增长的今日收入、工作节点倒计时、法定节假日区间进度，以及按需开启的退休和存款目标，给用户提供轻量、直观的“离下班和自由还有多远”视图。

核心原则：

- 默认无需账户、无需服务器、无需联网即可使用。
- 薪资、退休和存款等敏感数据始终保存在本机。
- App 前台每秒刷新时间相关数据；重新进入前台时按当前时间重新计算，不依赖后台持续运行。
- 计算结果主要用于个人激励和娱乐，不构成薪资、退休或财务建议。

## 2. V1 平台范围

- 使用 SwiftUI 开发原生 iPhone 应用。
- 最低支持 iOS 26，不兼容 iOS 25 及更早版本。
- 竖屏优先，横屏需要可用，但不做独立横屏设计。
- 提供 iPhone 主屏幕小组件与锁屏小组件。
- V1 不提供 iPad、Mac、Apple Watch 独立界面。
- V1 不提供 Live Activity。

当前开发环境仅安装了 Swift 6.2 命令行工具，尚未安装或启用完整 Xcode。创建、编译、运行及签名 iOS 工程前需要安装 Xcode。

## 3. 首次设置

首次启动至少完成以下设置：

1. 薪资输入方式。
2. 每周固定工作日。
3. 统一的上班、午休开始、午休结束和下班时间。

薪资和工作时间属于必填配置，首次设置不允许跳过。退休倒计时、存款和存款目标默认关闭，用户可以完全跳过并在设置中稍后开启。

### 3.1 薪资输入方式

V1 只支持以下两种方式：

1. 填写月薪和薪数，例如月薪 8,000 元、14 薪。
2. 直接填写年度总包；计算时先除以 12 得到月均收入。

统一换算：

```text
方式一：年度总包 = 月薪 × 薪数
方式二：年度总包 = 用户填写的年度总包
月均收入 = 年度总包 ÷ 12
```

额外薪平均摊入全年每个月和每个工作日，因此“今日收入”体现完整年度总包价值。

V1 删除日薪和时薪输入。未来增加医护轮班模式时，可重新评估时薪和按班次计薪。

默认值：

- 月薪：8,000 元
- 薪数：12 薪
- 每周工作日：周一至周五
- 上班时间：09:00
- 午休时间：12:00–13:00
- 下班时间：18:00

## 4. 工作日历

### 4.1 固定班次

V1 中，所有工作日共用一套由用户设置的：

- 上班时间
- 午休开始时间
- 午休结束时间
- 下班时间

每周固定工作日由用户选择。V1 不支持：

- 不同星期采用不同班次
- 跨午夜班次
- 一天多段班次
- 医护轮班或不规则排班

实现时必须通过可扩展的“班次策略”抽象计算逻辑，为未来的医护/护士灵活排班模式保留扩展空间，不能把固定班次规则散落并写死在界面中。

### 4.2 法定节假日、调休和手动覆盖

- 中国法定节假日与调休数据按年份打包进 App。
- 数据源采用 [NateScarlet/holiday-cn](https://github.com/NateScarlet/holiday-cn)，使用时保留 MIT License 归属信息。
- `holiday-cn` 中的法定休息日和调休工作日优先于用户的固定星期规则。
- 用户可以把某个具体日期手动覆盖为“上班”或“休息”；手动覆盖拥有最高优先级。

工作日判定优先级：

```text
手动日期覆盖 > holiday-cn 法定休息/调休 > 用户每周固定工作日
```

### 4.3 每月实际工作日

每月工作日数不能使用固定的 22 天，而应根据当月日历计算：

```text
当月实际工作日数
= 满足工作日判定规则的日历日期数量
```

计算结果按月至少缓存一次。缓存键必须包含：

- 年月
- 每周工作日配置版本
- 节假日数据版本
- 手动日期覆盖版本

以上任一配置变化时，使相关月份缓存失效并重新计算。若某月实际工作日数为 0，则禁止进行除法并显示配置错误。

### 4.4 手动更新节假日信息

设置中提供“更新节假日信息”选项。用户主动点击后：

1. 从 `holiday-cn` 提供的年度 JSON 地址下载当年和下一年度数据。
2. 分别校验 JSON schema、目标年份、日期格式、日期所属范围、名称和 `isOffDay` 字段。
3. 单个年份只有在完整校验通过后才原子替换对应的本地数据。
4. 下载或校验失败时保留现有内置/已下载数据，不得先删除旧数据。
5. 更新成功后使受影响月份的工作日缓存失效，并刷新收入、节日进度和小组件数据。
6. 设置页显示数据来源、覆盖年份、最后成功更新时间和本次更新结果。

该功能只下载结构化 JSON，不解析或爬取网页，不上传用户配置，也不执行后台定时更新。除用户主动更新节假日数据外，App 的核心功能无需联网。

如果本地没有下一年度数据且在线更新也未获得有效数据，节日区间停止计算并显示“下一年度假期安排待发布”，不得用普通周末代替法定节日节点。

## 5. 今日收入

### 5.0 金额格式

- V1 仅支持人民币，不提供币种切换。
- 金额输入最多允许两位小数。
- 今日收入、实时存款、月薪、年度总包和目标金额统一显示两位小数。
- 使用人民币符号与千位分隔，例如 `¥112,000.00`。
- 不使用“万”等缩写，避免丢失精度。

### 5.1 当日工资

```text
当月日工资 = 年度总包 ÷ 12 ÷ 当月实际工作日数
当日计薪总秒数 = 下班时间 - 上班时间
每秒收入 = 当月日工资 ÷ 当日计薪总秒数
```

午休不从计薪时长中扣除。默认 09:00–18:00 按完整 9 小时计薪，午休期间收入继续增长。

### 5.2 今日已入账

在工作日：

```text
上班前：今日已入账 = 0
上班至下班：今日已入账 = 当月日工资 × 已经过秒数 ÷ 当日计薪总秒数
下班后：今日已入账 = 当月日工资
```

在休息日：

```text
今日已入账 = 0
```

V1 不计算加班、迟到、请假、缺勤和加班工资。

## 6. 工作状态与倒计时

App 和小组件根据当前状态显示最近的重要节点：

| 当前状态 | 主倒计时 |
| --- | --- |
| 工作日上班前 | 距离上班 |
| 上午工作中 | 距离午休 |
| 午休中 | 距离午休结束 |
| 下午工作中 | 距离下班 |
| 下班后、周末或节假日 | 距离下一个工作日上班 |

补充规则：

- 午休只影响状态文案和主倒计时。
- 午休不暂停今日收入、今日工时进度或距离下班的总进度。
- App 首页可以在主倒计时之外继续显示完整的下班倒计时。

### 6.1 首页信息层级

首页自上而下按以下优先级展示：

1. 今日已入账，作为首页最大数字。
2. 当前节点倒计时，包括距离午休、下班或下次上班。
3. 今日工时与工作进度。
4. 法定节假日区间进度。
5. 按需开启的退休倒计时。

实时存款和存款目标不出现在首页，统一放入独立的“存款”页面。

### 6.2 导航结构

- 底部 Dock/Tab Bar 只包含“首页”和“存款”。
- 设置不占用底部入口，通过右上角设置按钮进入。
- “存款”入口始终固定显示；存款功能未启用时，页面展示空状态和“启用存款功能”按钮，入口位置不随配置改变。
- Dock 使用标准 SwiftUI `TabView` 构建，直接采用 iOS 26 系统原生动态 Liquid Glass。
- Dock 始终保持展开和可见，不随页面滚动收起或最小化。
- 不给系统 Tab Bar 叠加自定义不透明背景，避免破坏玻璃的透射、动态取色和交互反馈。

## 7. 小组件

- 小组件不显示今日收入、月薪、年度总包、存款或存款目标。
- 小组件只显示当前状态和对应倒计时。
- 倒计时使用系统支持的动态时间视图逐秒变化，不通过每秒重载 WidgetKit 时间线实现。
- 配置改变或日期状态改变时，由 App 刷新共享数据和小组件时间线。
- 小组件读取 App Group 中的最小必要非敏感配置快照。

V1 支持以下尺寸：

- 主屏幕小号：当前状态、逐秒倒计时和简洁进度。
- 主屏幕中号：当前状态、逐秒倒计时和今日工作进度。
- 锁屏矩形：当前状态与逐秒倒计时。
- 锁屏行内：极简状态与逐秒倒计时。

V1 暂不支持主屏幕大号和锁屏圆形小组件。

## 8. 法定节假日区间进度

目标是显示类似“端午节 → 中秋节”的区间进度，而不是普通周末倒计时。

### 8.1 假期段

- 从 `holiday-cn` 中选取 `isOffDay = true` 的节日日期。
- 将节日名称相同且日期连续的记录聚合为一个假期段。
- 普通周末不会单独成为节日节点，除非数据源将其作为具名假期记录。

### 8.2 区间进度

在两个假期之间：

- 上一个假期结束次日 00:00，进度为 0%。
- 下一个假期首日 00:00，进度达到 100%。
- 中间按真实日历时间连续推进，而不是仅统计工作日。
- 跨年时联合读取相邻年份的数据。

在假期期间：

- 不继续显示 100% 的“上一节日 → 当前节日”进度条。
- 改为显示“中秋假期中 · 第 1/3 天”一类状态。
- 假期结束后切换到下一个节日区间，例如“中秋节 → 国庆节”。

## 9. 退休倒计时

退休倒计时默认关闭，由用户按需开启。所有结果标注“仅供娱乐参考”。

### 9.1 自动估算

输入：

- 出生年月
- 人员类型：男职工、原 50 岁退休女职工、原 55 岁退休女职工

计算：

- 根据当前渐进式延迟法定退休年龄规则，离线计算对应的法定退休年月。
- 自动退休日期由出生年月和对应人员类型的渐进式退休年龄得出。
- 倒计时终点设为法定退休月结束后的次月 1 日 00:00，含义为完整度过最后一个工作月。
- 退休规则以内置、版本化数据或规则表实现，并记录官方来源与政策更新日期。
- 自动估算页显示当前政策版本/更新日期。
- 退休政策不通过网络动态下载；政策变化时随 App 新版本更新。
- 用户可以随时切换到手动退休模式，自动结果始终标注“仅供娱乐参考”。

### 9.2 手动估算

```text
输入精度 = 一位小数
换算天数 = round(用户填写的剩余年数 × 365.2425)
手动退休日期 = 设置日期 + 换算天数个日历日
```

换算结果四舍五入到整天。

### 9.3 平行展示口径

设距离退休终点的剩余日历天数为 `D`：

```text
剩余总年数 = floor(D ÷ 365.2425)
剩余总月数 = floor(D ÷ 30.436875)
剩余总天数 = D
```

年、月、天是三种平行总量，不展示为拆分后的“X 年 X 月 X 天”。

## 10. 实时存款与存款目标

存款和存款目标默认关闭，由用户按需开启。

### 10.1 期初存款和起算点

- 用户手动填写期初存款。
- 期初存款代表设置当天 00:00 的存款余额。
- 工资从设置当天的上班时间开始累计。
- 用户以后修改存款时，新金额成为修改当天 00:00 的新期初存款，同时重置工资回算起点，防止重复累计。

### 10.2 实时存款余额

```text
实时存款余额
= 期初存款
+ 起算日之后、今天之前的完整工作日工资
+ 今日已入账
```

完整工作日工资根据当前薪资配置回算；每个历史工作日使用其所在月份的实际工作日数计算当月日工资。

### 10.3 存款目标

用户只需设置一个目标存款金额：

```text
完成度 = min(实时存款余额 ÷ 目标存款金额, 100%)
```

界面显示：

- 当前实时存款余额
- 目标存款金额
- 距离目标还差多少
- 完成度
- 达标后的超出金额

V1 不预测预计达标日期。

### 10.4 已删除功能

V1 明确不包含：

- 心愿物品目标
- 目标月支出
- “撑到退休每天可花”
- “撑到退休每月可花”
- 与上述字段相关的彩蛋或衍生计算

## 11. 隐私模式

- 首页提供眼睛按钮，一键切换金额可见性。
- 隐私模式下，月薪/年度总包、今日收入、存款、目标金额和差额统一显示为 `••••`。
- 工时、倒计时、节假日进度、退休倒计时和非金额百分比仍可显示。
- App 进入后台时，无论隐私模式是否开启，都使用隐私遮罩保护应用切换器快照。
- V1 不提供用假金额替换真实金额的独立“演示模式”。

## 12. 本地数据与配置管理

- 所有用户配置和敏感数据只保存在本机。
- App 与小组件通过 App Group 共享小组件所需的最小数据。
- 配置支持直接导出为带 schema version 的明文 JSON 文件，不加密、不设置密码。
- 导出文件包含恢复完整配置所需的薪资、退休、存款等敏感字段；导出前明确提示文件含敏感信息，只有用户确认后才打开系统分享面板。
- 用户主动导出后的文件保存位置和分享范围由用户负责；App 不自动上传或同步导出文件。
- 导入前必须验证文件格式、版本、字段范围、时间顺序和数值合法性；校验失败不得覆盖现有配置。
- 清除全部数据前必须二次确认。
- 导入成功后重新计算工作日缓存、收入、存款和小组件快照。

### 12.1 数据收集

- 不接入第三方统计、广告或崩溃收集 SDK。
- 不创建账户，不采集设备标识。
- 不记录或上传用户薪资、存款、退休信息和使用行为。
- 只使用用户在系统设置中自愿共享给开发者的 Apple 崩溃与性能诊断。
- App Store 隐私标签计划按“不收集数据”申报；正式提交前必须根据最终二进制和网络行为再次审计。

## 13. 架构约束

- 时间和金额计算必须与 SwiftUI View 解耦，可通过确定的 `Date` 输入进行单元测试。
- 所有“当前值”应由基准配置与当前时间推导，不通过每秒持久化数值实现。
- 金额内部使用十进制高精度类型，避免浮点误差；展示层再统一格式化。
- 日历计算明确使用用户当前时区和 Gregorian Calendar。
- 固定班次通过策略接口实现，为未来医护轮班模式预留新策略，而不修改现有核心公式。
- 配置导出格式必须包含 schema version，以便后续迁移。

## 14. 视觉与交互质量

### 14.1 视觉主张

界面应是一块克制、安静但略带幽默感的个人时间仪表盘：以大号等宽数字和清晰留白为视觉锚点，以一种强调色表达进行中状态，以系统动态玻璃承载导航和关键操作。

品牌色采用“暖白 / 炭黑 + 柿子橙”：

- 浅色模式使用暖白背景与炭黑文字，避免冷灰工具感。
- 深色模式使用接近墨黑的背景与暖白文字。
- 柿子橙是唯一主强调色，只用于收入增长、当前进度和关键操作。
- 不使用蓝紫渐变或多彩仪表盘风格。

App 图标采用暖白底色、柿子橙时间进度环与炭黑指针，以“时间推进”作为主意象，不使用鱼或人民币符号。当前图标作为 V1 实现稿，可在发布前根据真机效果微调。

### 14.2 组合原则

- 首页第一屏以“今日已入账”作为唯一主视觉，不使用卡片矩阵。
- 倒计时、工作进度、节假日和退休信息通过字号、间距、细分隔线与少量色彩建立层级。
- 默认避免给每个模块添加描边、阴影、渐变背景和圆角容器；只有可交互或需要与背景分层的区域才使用容器。
- 全局最多使用一种主强调色；正文和次要信息使用系统语义色。
- 使用系统字体与等宽数字特性，优先保证金额和倒计时稳定、易扫读。
- 深色模式、提高对比度、减少透明度和减少动态效果时仍须清晰可用。
- 动态玻璃主要用于 Dock、工具栏和关键控件，不铺满内容区域。

### 14.3 动效主张

- 今日收入使用克制的数字内容过渡，逐秒变化时不引发布局抖动。
- 当前节点切换时使用短促的状态过渡，使文案、倒计时和强调色同步变化。
- Dock 使用 iOS 26 系统 Liquid Glass 的触摸响应，并始终保持展开。
- 进度变化保持平滑，但不为每秒刷新添加夸张弹跳、粒子或持续装饰动画。
- 用户开启“减少动态效果”时，所有非必要过渡退化为淡入淡出或直接更新。

### 14.4 通知、声音与触觉

- V1 不申请通知权限，不发送上班、午休或下班提醒。
- V1 不播放提示音。
- 仅在保存配置、切换隐私模式、完成存款目标等明确操作或状态中使用轻微触觉反馈。
- 遵循系统触觉、减少动态效果等辅助功能设置。

### 14.5 语言与无障碍

- V1 只提供简体中文。
- 完整支持浅色和深色模式。
- 支持 Dynamic Type；超大字号时允许页面自然滚动，不截断核心数据。
- 金额和倒计时提供自然、明确的 VoiceOver 读法，不能逐字符机械朗读。
- 支持“减少动态效果”“降低透明度”和“提高对比度”。
- 不提供专门的横屏界面，但横屏下所有内容和操作必须可用。

## 15. V1 验收要点

至少覆盖以下自动化测试场景：

1. 普通工作日的上班前、上午、午休、下午和下班后状态。
2. 午休期间收入持续增长。
3. 普通周末、法定休息日、法定调休工作日和手动覆盖日期。
4. 具有不同实际工作日数的月份，其日工资正确变化。
5. 跨月、跨年及夏令时变化时倒计时不出现负数或重复累计。
6. 节日区间、假期进行中及跨年节日区间。
7. 12 薪、14 薪和直接填写年度总包得到一致的归一化结果。
8. 自动和手动退休日期及年/月/天平行口径。
9. 修改期初存款后正确重置起算点。
10. 存款目标未达标、刚好达标和超额三种状态。
11. 隐私模式及进入后台后的应用切换器遮罩。
12. 配置导出、合法导入、非法导入拒绝和清除确认。
13. 小组件在各工作状态下显示正确的动态倒计时且不泄露金额。

## 16. 响应式布局

- 不提供独立的“紧凑布局”设置。
- 界面根据屏幕可用高度和 Dynamic Type 自动调整。
- 空间充足时展示完整标签、辅助说明和舒展间距。
- 空间不足时优先缩短辅助文案并收紧间距，但不得隐藏核心数据。
- 不通过过度缩小字号牺牲可读性；内容超出时允许自然滚动。
- Liquid Glass Dock 不参与紧凑布局，始终保持展开和可见。

## 17. 待继续确认

以下内容尚未在需求访谈中锁定：

- 发布方式和商业模式。
